#!/bin/bash

# IMPORTANT: Do not use this file directly to store your secrets
# create a copy in the same dir called 'private-env'

# GitOps repo settings
export GITOPS_REPO_URL=
export GITOPS_TARGET_REVISION=
export ODH_SETUP_DIR=
export RHDH_NAMESPACE=rolling-demo-ns
export ARGOCD_APP_NAME=rolling-demo

# Github secrets
export GITHUB_APP_APP_ID=
export GITOPS_GIT_TOKEN=
export GITOPS_GIT_ORG=
export GITHUB_APP_CLIENT_ID=
export GITHUB_APP_CLIENT_SECRET=
export GITHUB_APP_WEBHOOK_URL=
export GITHUB_APP_WEBHOOK_SECRET=
export GITHUB_APP_PRIVATE_KEY=

# ArgoCD secrets
export ARGOCD_USER=

# RHDH secrets
export BACKEND_SECRET=
export RHDH_CLUSTER_ROUTER_BASE=

# RHDH Postgres Secrets
export POSTGRESQL_POSTGRES_PASSWORD=
export POSTGRESQL_USER_PASSWORD=

# Quay.io secrets
export QUAY_DOCKERCONFIGJSON=

# KeyCloak (RH SSO) secrets
export KEYCLOAK_METADATA_URL=
export KEYCLOAK_CLIENT_ID=
export KEYCLOAK_REALM=
export KEYCLOAK_BASE_URL=
export KEYCLOAK_LOGIN_REALM=
export KEYCLOAK_CLIENT_SECRET=

# Llama Stack secrets (for Lightspeed Core Service)
export VLLM_URL=
export VLLM_API_KEY=
export VALIDATION_PROVIDER=
export VALIDATION_MODEL_NAME=
export NOTEBOOKS_QUERY_PROVIDER_ID=
export NOTEBOOKS_QUERY_MODEL=

# Postgres secrets
export LIGHTSPEED_POSTGRES_PASSWORD=
export LIGHTSPEED_POSTGRES_USER=
export LIGHTSPEED_POSTGRES_DB=

# RBAC / Permission framework
export PERMISSION_ENABLED=
export RBAC_ADMIN_USERS=

# Orchestrator — RHDH Orchestrator plugin + infrastructure
export INSTALL_ORCHESTRATOR=
export ORCHESTRATOR_INFRA_VERSION=
# Path to local rhdh-plugins clone (for deploying SonataFlow agent-approval workflow)
export AUGMENT_PLUGINS_DIR=

# Augment plugin — provider selection (llamastack or kagenti)
export AUGMENT_PROVIDER=

# Augment plugin secrets — LlamaStack Responses API provider
export AUGMENT_LLAMA_STACK_URL=
export AUGMENT_MODEL=

# Augment plugin secrets — Kagenti AgenticProviders
export KAGENTI_BASE_URL=
export KAGENTI_NAMESPACE=
export KAGENTI_TOKEN_ENDPOINT=
export KAGENTI_CLIENT_ID=
export KAGENTI_CLIENT_SECRET=